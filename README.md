# Canvas

A small, static gallery page that links to pieces (these are also on my GH). It uses only HTML, CSS, and a bit of JavaScript - no build steps or dependencies.

## What’s inside
- Custom cursor and smooth scroll-reveal animations.
- Live iframe previews

## How to view
1. Open `index.html` directly in a browser
